# CASA application

This is the active Next.js application directory for CASA.

The submission-ready project documentation is maintained at the Git repository root: [`../README.md`](../README.md). Use this directory for `npm install`, `npm run dev`, `npm run lint`, and `npm run build`.
