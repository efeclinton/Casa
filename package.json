{
  "name": "casa-workspace",
  "private": true,
  "workspaces": [
    "casa"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=casa",
    "build": "npm run build --workspace=casa",
    "start": "npm run start --workspace=casa",
    "lint": "npm run lint --workspace=casa"
  }
}
